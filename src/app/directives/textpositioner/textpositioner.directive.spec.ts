import { TextpositionerDirective } from './textpositioner.directive';

describe('TextpositionerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextpositionerDirective();
    expect(directive).toBeTruthy();
  });
});
